[M] OfficerLogin.tsx @ styles.css .masthead .top (lines 494-499), rendered by Band in OfficerLogin.tsx line 32
    PROBLEM: The masthead row is `display:flex; justify-content:space-between` with no wrap/stack rule at any breakpoint. On this page the right cell `.ref` holds two no-wrap mono strings ('OFFICER · لوحة الموظف' and 'MIZAN / ميزان') while the left cell holds the h1 'Officer Dashboard'. At 320-375px the two colu
    FIX: Add to the existing styles.css media block (e.g. @media (max-width: 600px)): `.masthead .top { flex-direction: column; gap: var(--s2); } .masthead .ref { text-align: left; white-space: normal; } [dir="rtl"] .masthead .ref { text-align: right; }`. Stacking the title above the ref removes the column c

[M] OfficerLogin.tsx @ styles.css .masthead .ref (lines 507-513), fileRef passed at OfficerLogin.tsx line 32
    PROBLEM: `.ref` is `font-family: var(--mono); white-space: nowrap`. The Arabic/Latin mono string 'OFFICER · لوحة الموظف' cannot break, so on a 320px viewport it is a guaranteed horizontal-overflow source whenever it does not fit beside the h1.
    FIX: Allow wrapping on small screens via the same media query: `.masthead .ref { white-space: normal; word-break: break-word; }`. Pairs with the .top stacking fix above; keep the desktop nowrap rule unchanged outside the media query.

[M] OfficerLogin.tsx @ OfficerLogin.tsx line 34-37 (`.lead`), styles.css .lead lines 564-571
    PROBLEM: `.lead` uses `font-size: clamp(18px, 2.2vw, 22px)`. The clamp minimum is 18px which is acceptable, but the two-line lead with `max-width: 40ch` is comfortable; this is borderline-only. The real (minor) concern is that 18px lead text sits fine but leaves little vertical room above the 480px card on s
    FIX: Optional: in @media (max-width: 480px) reduce to `.lead { font-size: 16px; line-height: 1.45; }` for tighter mobile reading. Not required to prevent overflow; the clamp min is already mobile-safe.

[M] CitizenLogin.tsx @ styles.css .masthead .top (lines 494-499) and .masthead .ref (lines 507-513); rendered by 
    PROBLEM: The masthead header is a flex row (display:flex; justify-content:space-between) that never stacks. The right-hand .ref has white-space:nowrap and contains two long mono strings ('CITIZEN · بوابة المستفيد' and 'MIZAN / ميزان'). On a 320-375px viewport the nowrap ref cannot shrink, so it collides with
    FIX: Add to the @media (max-width: 600px) block in styles.css: `.masthead .top { flex-direction: column; align-items: flex-start; gap: var(--s2); }` and `.masthead .ref { text-align: left; white-space: normal; }` ([dir=rtl] .masthead .ref text-align:right). Optionally tighten the h1 floor, e.g. `.masthea

[M] CitizenLogin.tsx @ styles.css .seg button (lines 282-296); used in CitizenLogin.tsx lines 92-99 (Account / UA
    PROBLEM: The segmented toggle buttons use padding:5px 13px with font-size:12px, giving a tap target only ~26-28px tall — below the 40px minimum recommended for touch. Affects all phone widths.
    FIX: In the @media (max-width: 600px) block add: `.seg button { padding: 9px 16px; font-size: 13px; }` so the toggle reaches ~40px tall. Optionally `.seg { width: 100%; } .seg button { flex: 1; }` to make the two-option toggle full-width and easier to tap. Global CSS only.

[M] CitizenLogin.tsx @ CitizenLogin.tsx lines 126-145 (accounts demo list: inline <code> chips inside .caption, s
    PROBLEM: Each demo account username is a clickable inline <code> element (lines 131-140) with no padding — it is a tiny inline-text tap target (~16px tall) sitting in a wrapping run of text separated by middots. On phones these are very hard to tap accurately and the wrapped ' · ' separators look messy.
    FIX: Make the clickable usernames real tap targets. Preferred (JSX change in CitizenLogin.tsx): give the clickable <code> a class (e.g. className="acct-chip") and add a global rule `.acct-chip{display:inline-flex;align-items:center;min-height:32px;padding:4px 10px;margin:3px;border:1px solid var(--line-2

[M] CitizenLogin.tsx @ CitizenLogin.tsx line 91: <div className="card tab" style={{ marginTop: 24, maxWidth: 560 
    PROBLEM: Not a breakage, but verify: the card has a fixed inline maxWidth:560 (caps correctly below viewport) and inherits .card padding var(--s6)=24px (lines 524-531). At 320px the card is full width minus the .main side padding (20px each) so content width is ~280px, which is acceptable. No overflow here —
    FIX: No change required; the maxWidth:560 + percentage-based inputs (select/input width:100%) scale down correctly. Listed for completeness — safe to leave as-is.

[M] Home.tsx @ Home.tsx lines 42-49 (intro <video style={{ width:"100%", height:"100%", objectFit:"cover"
    PROBLEM: objectFit:cover on a full-viewport video means a landscape /intro.mp4 is heavily cropped on a 320-480px portrait phone — viewers may see only a tiny center slice, and on devices where autoplay is blocked the muted video may not start, leaving a black screen. The only escape is the Skip button.
    FIX: Acceptable to keep cover, but make the dismiss affordance reliable on phones: the whole overlay already has onClick={dismissIntro} so tap-to-dismiss works — keep it. Optionally add objectFit:'contain' with a black background on very small screens, or simply rely on the existing full-overlay tap. No 

[M] Home.tsx @ Home.tsx line 53: Skip button style={{ position:"absolute", bottom: 32, right: 32 }}
    PROBLEM: On a 320px phone the bottom:32 / right:32 places the Skip pill near the home-indicator / gesture bar area on iOS; it can sit under the gesture inset and be hard to tap. Also it does not account for safe-area insets.
    FIX: Use safe-area-aware offsets, e.g. bottom: 'max(32px, env(safe-area-inset-bottom))' and right: 'max(20px, env(safe-area-inset-right))', or add a global rule scoping a class. Since whole-overlay tap also dismisses, this is low risk; primary fix is just ensuring the button clears the inset.

[M] Home.tsx @ styles.css .masthead .top (lines 494-499) combined with .masthead .ref (lines 507-513, whi
    PROBLEM: Hero header row is display:flex; justify-content:space-between with NO flex-wrap. The right-hand .ref block is white-space:nowrap mono ("FILE · القضية" / "MIZAN / ميزان") sitting beside the clamp(34px,5.5vw,56px) h1. At 320-375px the nowrap ref plus the large h1 force horizontal overflow / squeeze t
    FIX: Add to the @media (max-width: 600px) block in styles.css: .masthead .top { flex-direction: column; align-items: flex-start; gap: var(--s2); } .masthead .ref { text-align: left; } [dir=rtl] .masthead .ref { text-align: right; } This stacks the ref under the title and removes the nowrap overflow. No J

[M] Home.tsx @ styles.css .masthead h1 (lines 500-506) font-size: clamp(34px, 5.5vw, 56px)
    PROBLEM: Minimum clamp floor is 34px. With max-width:20ch a long title ("An autonomous case officer") at 34px on a 320px viewport wraps to several tall lines but is borderline acceptable; combined with the non-wrapping .top row above it contributes to crowding. The 34px floor itself is on the large side for 
    FIX: Optionally lower the floor for tiny screens inside @media (max-width: 380px): .masthead h1 { font-size: clamp(28px, 8vw, 34px); }. Primary fix is the .top stacking above; this is secondary polish.

[M] Home.tsx @ styles.css .topbar (lines 176-188) padding: 18px clamp(20px,5vw,64px) and .brand .seal (li
    PROBLEM: The shared topbar (rendered on every page, not in Home.tsx itself) has a 96px-tall brand seal. The @media (max-width:1040px) makes .topbar wrap and pushes .topnav to a full-width third row, but the seal stays 96px tall, making the mobile header very tall and eating vertical space above the Home hero
    FIX: In @media (max-width: 600px) add: .brand .seal { height: 56px; } (and optionally .footer .f-brand .seal { height:64px }). Global CSS only.

[M] Home.tsx @ styles.css .grid-2 / .grid-3 (lines 949-954) and the @media (max-width:900px) collapse (li
    PROBLEM: NOT a bug — verified OK. grid-2 (portal cards) and grid-3 (how-it-works) both collapse to a single column at max-width:900px, which covers all phones (320-600) and small tablets. No change needed; listed only to confirm it was checked.
    FIX: No change required. The collapse breakpoint at 900px adequately handles 320-600px phones.

[M] Home.tsx @ Home.tsx lines 88, 103, 125 — inline style={{ fontSize: 22 }} / 20 }} on card <h4> heading
    PROBLEM: Inline 22px/20px heading sizes are fine on phones and do not overflow. Listed only to confirm: these are not responsiveness problems and need no media query.
    FIX: No change required.

[M] Home.tsx @ styles.css .main padding (line 384): padding: clamp(28px,5vw,56px) clamp(20px,5vw,64px) 96
    PROBLEM: Horizontal padding floor is 20px each side (40px total). On a 320px viewport that leaves ~280px content width, which is acceptable for the single-column cards. Confirmed not a problem; the clamp minimums are mobile-appropriate.
    FIX: No change required. Minimums (20px / 28px) are mobile-appropriate.

[M] Replay.tsx @ styles.css .bar-row (lines 915-921) + @media(max-width:900px) (lines 1137-1139); used by R
    PROBLEM: The outcome bars use a fixed-px grid 'grid-template-columns: 200px 1fr 36px' (collapsing only to '130px 1fr 32px' at <=900px). The .lbl is a mono outcome key like 'pending_human_review' / 'straight_through_partial_restructure'. At 320-375px the 130px label column is far too narrow for these long mon
    FIX: Add a <=600px media query in styles.css that stacks the row: '.bar-row{grid-template-columns:1fr auto;grid-template-areas:"lbl num" "bar bar";row-gap:6px} .bar-row .lbl{grid-area:lbl;overflow-wrap:anywhere} .bar-row .num{grid-area:num} .bar-row .bar-track{grid-area:bar}'. Alternatively keep one row 

[M] Replay.tsx @ styles.css .brand .seal (lines 199-204), .footer .f-brand .seal (line 406-408); rendered i
    PROBLEM: The brand seal logo is locked to height:96px with no responsive reduction. In the topbar (which wraps at <=1040px) a 96px-tall image makes the header occupy a huge slice of a 320-568px-tall phone viewport before any Replay content is visible, and crowds the wrapped nav. The footer seal is also 96px.
    FIX: In styles.css add to the <=600px (and ideally <=900px) media query: '.brand .seal{height:56px} .footer .f-brand .seal{height:64px}'. This is a pure CSS change, no markup edit.

[M] Replay.tsx @ Replay.tsx lines 54-74 (table.tbl wrapped in <div style={{overflowX:'auto'}}>) + styles.cs
    PROBLEM: The cases table renders every key of the first case object as a column with mono cells, so on a 320-480px phone it is much wider than the viewport. It is correctly wrapped in an overflowX:auto div so the page itself does not break, but the table has no min-width/affordance and on iOS the scroll cont
    FIX: Acceptable as-is since the wrapper scrolls. To harden, add in styles.css: 'table.tbl{min-width:100%} table.tbl th,table.tbl td{white-space:nowrap}' so columns keep their integrity inside the scroll area, and add '-webkit-overflow-scrolling:touch' to the scroll wrapper (would require a className on t

[M] Replay.tsx @ styles.css .masthead .top (lines 494-499) and .masthead .ref (lines 507-513); rendered by 
    PROBLEM: The masthead uses display:flex; justify-content:space-between and never wraps, while .ref has white-space:nowrap ('FILE · القضية' / 'MIZAN / ميزان'). At 320-375px the nowrap ref column competes with the h1 (clamp min 34px, max-width 20ch 'Replay Dashboard'), squeezing the heading and risking the two
    FIX: Add to the <=600px media query in styles.css: '.masthead .top{flex-direction:column} .masthead .ref{text-align:left}' (and for RTL '[dir="rtl"] .masthead .ref{text-align:right}'). Pure CSS, no JSX change.

[M] Replay.tsx @ styles.css .metric .v (lines 759-768) font-size:var(--fs-xl)=32px; Replay.tsx Metric tiles
    PROBLEM: Metric values render at 32px. After grid-4 collapses to 1 column at <=900px the tiles are full width so 32px usually fits, but large values such as 'estimated_manual_working_days_saved' or a thousands-separated 'total_cases' at 32px in tabular-nums can still approach the edge on a 320px viewport wit
    FIX: In the <=600px media query add '.metric .v{font-size:26px}' (or use 'clamp(24px,7vw,32px)') so big metric numbers never overflow narrow tiles. CSS only.

[C] OfficerCase.tsx @ OfficerCase.tsx line 98: <div className="grid" style={{ gridTemplateColumns: "1fr auto aut
    PROBLEM: The Case ID / Officer ID / Load row is forced to a single 3-column row via an INLINE gridTemplateColumns. On 320-480px this crams two text inputs and a button into one line: inputs shrink to ~80px and become unusable/overflow, and there is no media query that collapses it (the global @media collapse
    FIX: Because inline style beats CSS, the cleanest fix is a JSX change: remove the inline gridTemplateColumns and add a reusable class, e.g. className="grid grid-loadbar". Then in styles.css define `.grid-loadbar{grid-template-columns:1fr auto auto;align-items:end}` and add inside the existing `@media (ma

[C] OfficerCase.tsx @ OfficerCase.tsx line 117: <div className="grid" style={{ gridTemplateColumns: "3fr 2fr", m
    PROBLEM: The main two-pane layout (left = profile/plans/policy/evidence, right = confidence + Decision panel) is pinned to a 3fr/2fr two-column grid via INLINE gridTemplateColumns. At 320-1024px the right 'Decision' column collapses to ~40% width: the textarea, select, and 'Approve / Apply override / Reject'
    FIX: JSX change required: replace the inline gridTemplateColumns with a class, e.g. className="grid grid-case", and in styles.css add `.grid-case{grid-template-columns:3fr 2fr}` plus, inside `@media (max-width:900px)`, `.grid-case{grid-template-columns:1fr}` so the two panes stack vertically on tablet/ph

[M] OfficerCase.tsx @ OfficerCase.tsx line 187: <div className="grid grid-2" style={{ marginTop: 8 }}> (New inst
    PROBLEM: Two number inputs side-by-side via grid-2. This one DOES collapse to 1 column at <=900px via the global rule, so it is fine on phones/large-phones. But note it lives inside the already-narrow right pane (the 3fr/2fr issue above), so before the right pane is fixed these inputs are doubly squeezed on 
    FIX: No change needed once the line-117 right pane stacks. grid-2 already collapses to 1fr at max-width:900px in styles.css.

[M] OfficerCase.tsx @ styles.css .brand .seal (lines 199-204): height:96px; — rendered in the shared topbar/foot
    PROBLEM: The brand seal logo is a fixed 96px tall. In the topbar (padding 18px) this makes the header ~132px tall on a 320-375px phone, eating a large chunk of the viewport, and combined with the wrapping nav (topnav goes full width at <=1040px) the masthead is pushed well down. The footer .f-brand .seal is 
    FIX: In styles.css add to a phone media query (e.g. @media (max-width:600px)): `.brand .seal{height:56px} .footer .f-brand .seal{height:64px}` to reclaim vertical space. Optionally reduce topbar padding too: `.topbar{padding:12px clamp(14px,5vw,20px)}`.

[M] OfficerCase.tsx @ ui.tsx ConfidenceBlock lines 363 & 376: <div style={{ fontFamily: 'var(--mono)', fontSize:
    PROBLEM: Two big 32px mono metric numbers sit inside ConfidenceBlock's grid grid-2. The grid-2 collapses to 1 column at <=900px (good), but the 32px font is large for 320px and, combined with the .v metric (32px, var(--fs-xl)) in ProfileCard, makes the page feel oversized on small phones. Not an overflow bre
    FIX: These inline 32px values can't be hit by CSS without !important. Preferred: in styles.css scale the token-driven metric down on phones — add to @media (max-width:480px): `.metric .v{font-size:26px}`. For the two inline 32px ConfidenceBlock numbers, change them in ui.tsx to a class (e.g. className="b

[M] OfficerCase.tsx @ ui.tsx DocumentPreview lines 81-89: <object ... height={height} (default 360)> rendered in
    PROBLEM: Inline PDF <object>/<iframe> previews are a fixed 360px tall and width:100%. On phones the PDF renders too small to read and the fixed height wastes space; also embedded PDF viewers are unreliable on mobile Safari/Chrome (often blank), leaving the user only the small 'open in new tab' link. Not hori
    FIX: Optional: on small screens reduce/auto the height and lean on the 'open in new tab' link. Since height is inline JSX, either pass a smaller height on mobile or wrap the object in a class (e.g. .pdf-preview) and add `@media(max-width:600px){.pdf-preview object,.pdf-preview iframe{height:240px}}`. Ens

[M] OfficerCase.tsx @ ui.tsx PolicyTable lines 272-282 (gridTemplateColumns: '22px 1fr') and PlanCards .kv.mono 
    PROBLEM: Long mono strings can cause horizontal overflow on 320px: (a) pre.raw (line 137 raw_text) uses white-space:pre-wrap + overflow-x:auto so it scrolls internally — acceptable. (b) The PlanCards mono line 'AED x · y% of income · n months' and the 'Extracted: income=… employer=…' caption (OfficerCase lin
    FIX: Add a defensive global rule in styles.css: `.card, .kv, .caption{overflow-wrap:anywhere}` (or `word-break:break-word`) so long mono tokens/IDs wrap instead of overflowing. pre.raw already handles its own scroll, no change needed there.

[C] OfficerQueue.tsx @ OfficerQueue.tsx lines 41-44: <div className="grid" style={{ gridTemplateColumns: "1.6fr 1
    PROBLEM: This is the core layout of every queue card and it is the worst small-screen offender. The 4-track row (name+case-id block, Arrears metric, Confidence metric, Open button) is set via an INLINE style, so the existing @media(max-width:900px) rule that collapses .grid-2/.grid-3/.grid-4 does NOT apply (
    FIX: Because the column template is an inline style it overrides any class rule via specificity-free inline priority, so a plain global media query on .grid will NOT win. Best fix is a JSX change: remove the inline gridTemplateColumns and instead use a dedicated class, e.g. className="grid queue-card-gri

[M] OfficerQueue.tsx @ OfficerQueue.tsx line 53-55: <button className="btn primary"> as 4th grid track; also glob
    PROBLEM: The 'Open →' button lives in the 'auto' grid track. On phones (<=480px) the three preceding fr-tracks consume almost all width, leaving the auto track so narrow the button text wraps or the button shrinks below a comfortable tap target. While .btn height (~40px with 11px vertical padding) is accepta
    FIX: Covered largely by the grid fix above (giving the button its own full-width row under <=600px). Additionally, in @media(max-width:600px) add: .queue-card-grid .btn{width:100%;justify-content:center} so the button becomes a comfortable full-width tap target. Optionally add white-space:nowrap to keep 

[M] OfficerQueue.tsx @ OfficerQueue.tsx line 46: <div style={{ fontFamily: "var(--serif)", fontSize: 20, fontWeig
    PROBLEM: Inside the cramped first grid track, a long beneficiary_name_en at 20px and the monospace case_id (e.g. 'SZHP-1001-...') do not wrap on the mono string and can push the card wider than a 320px viewport, contributing to horizontal overflow. The 20px name combined with the 3 other columns is too large
    FIX: Once the name block spans the full row (grid fix above) the 20px size is fine. To be safe against long unbroken case-id/mono strings causing overflow, add globally: .card .mono{overflow-wrap:anywhere;word-break:break-word} (or scope to .caption.mono). No JSX change needed for this part.

[M] OfficerQueue.tsx @ styles.css lines 199-204: .brand .seal { height: 96px }  (shared topbar brand seal, and .f
    PROBLEM: The brand seal is a fixed 96px tall image. This shared chrome wraps above the OfficerQueue content. At <=480px a 96px-tall seal makes the sticky topbar very tall (the topbar already flex-wraps at <=1040px, pushing nav to its own row), eating a large fraction of a short phone viewport before any queu
    FIX: Add to the existing @media(max-width:600px) block in styles.css: .brand .seal{height:56px} and .footer .f-brand .seal{height:64px}. Pure global CSS, no JSX change.

[M] OfficerQueue.tsx @ styles.css lines 491-513: .masthead .top (flex space-between) + .masthead .ref (white-spac
    PROBLEM: OfficerQueue renders <Band> -> .masthead. .masthead .top is a flex row with justify-content:space-between and the right-hand .ref uses white-space:nowrap ('FILE · القضية' / 'MIZAN / ميزان'). On phones the nowrap ref sits beside the h1 (clamp min 34px) and the eyebrow; the two fight for width and the
    FIX: Add to @media(max-width:600px) in styles.css: .masthead .top{flex-direction:column;align-items:flex-start;gap:var(--s2)} and .masthead .ref{text-align:left;white-space:normal} (and for RTL, [dir=rtl] .masthead .ref{text-align:right}). Global CSS only.

[M] OfficerQueue.tsx @ styles.css lines 759-768: .metric .v { font-size: var(--fs-xl) } where --fs-xl = 32px (lin
    PROBLEM: The two metrics on each queue card (Arrears AED amount, Confidence %) use .metric .v at a fixed 32px. Combined with the not-yet-collapsed inline grid, a value like 'AED 124,500' at 32px tabular-nums will not fit a ~120px track on a 320-375px screen and overflows. Even after the grid is collapsed to 
    FIX: Add to @media(max-width:600px) in styles.css: .metric .v{font-size:26px} (or use a clamp on --fs-xl). This relieves overflow pressure inside the metric tiles on the queue cards. Global CSS only.

[M] Layout.tsx @ styles.css .brand .seal (lines 199-204) and .footer .f-brand .seal (lines 406-408); render
    PROBLEM: The brand seal is a fixed height: 96px and is never reduced at any breakpoint. In the topbar (which also has padding: 18px top/bottom) this forces the header to roughly 96+36 = ~132px tall on a 320-375px phone, eating a huge slice of the viewport and pushing content down. width:auto means a wide log
    FIX: Add to a phone media query in styles.css: `@media (max-width: 600px){ .brand .seal{ height:56px } .footer .f-brand .seal{ height:64px } }` (and optionally height:72px in a 600-1024 range). Also reduce topbar vertical padding on phones (see topbar finding). No JSX change needed.

[M] Layout.tsx @ styles.css .topbar (lines 176-188), responsive block lines 1116-1130; markup header.topbar
    PROBLEM: Below 1040px the topbar wraps and the nav drops to its own full-width row, but the horizontal padding stays at `clamp(20px,5vw,64px)` (min 20px each side) and vertical padding stays 18px. Combined with the 96px seal the header is very tall. There is also no rule reducing padding on phones, so on a 3
    FIX: In a `@media (max-width:600px)` block add: `.topbar{ padding:10px 16px; gap:var(--s2) } .main{ padding-left:16px; padding-right:16px }`. This shortens the header and gives content edge breathing room.

[M] Layout.tsx @ styles.css .topbar-actions (lines 267-272); markup div.topbar-actions in Layout.tsx lines 
    PROBLEM: `.topbar-actions` is `display:flex; flex:none` with NO flex-wrap. It contains the statuschip (~120px), the usermenu pill (avatar + name + role + Sign out button, easily 200px+) and the language seg. With flex:none it never shrinks. On a phone where the topbar has wrapped, this single row of actions 
    FIX: In styles.css add `@media (max-width:600px){ .topbar-actions{ flex-wrap:wrap; gap:var(--s2); width:100% } }`. Optionally hide the verbose status text on phones: `.topbar .statuschip{ font-size:10px }` and consider truncating `.usermenu .who-name{ max-width:18ch; overflow:hidden; text-overflow:ellips

[M] Layout.tsx @ styles.css .footer .f-base (lines 448-460) + .footer .f-logos (lines 461-474); markup div.
    PROBLEM: The f-base bottom bar is a flex row with justify-content:space-between containing the copyright, .f-logos (two logo chips at height:84px, padding 12px 20px, gap:32px between them) and the hc-toggle. The two logo chips alone are roughly 2x(~120px)+32px gap ≈ 270px+, and although the row has flex-wrap
    FIX: In styles.css `@media (max-width:600px)` block add: `.footer .f-base{ flex-direction:column; align-items:flex-start; gap:var(--s4) } .footer .f-logos{ gap:var(--s4); flex-wrap:wrap } .footer .f-logos img{ height:56px; padding:8px 14px }`. This stacks the three groups and shrinks the logo chips to fi

[M] Layout.tsx @ styles.css .footer-inner (lines 396-402) and responsive blocks lines 1127-1129 (1040px → 1
    PROBLEM: The 5-column footer grid (1.4fr repeat(4,1fr)) only collapses to two columns at 1040px and to one column at 600px. That mostly works, but in the 480-600px large-phone band it is still two columns with gap var(--s8)=32px; two link columns plus a 32px gutter is acceptable but the 32px gap is generous 
    FIX: Optional: in the 1040px block reduce footer gap, e.g. add `.footer-inner{ gap:var(--s6) }` for the 2-col stage, or move the single-column collapse breakpoint up to 640px so large phones get one clean column. No markup change.

[M] Layout.tsx @ styles.css .usermenu .signout (lines 362-373) padding 6px 11px → ~28px tall; .seg button (
    PROBLEM: Several primary interactive controls in the topbar are below the ~44px recommended touch target on phones: the Sign out button (~28px tall), the EN/ع language seg buttons (~26px tall and narrow), and these are the main tap targets a mobile user reaches for. Small targets are hard to hit on touch.
    FIX: In a `@media (max-width:600px)` block bump tap sizes: `.usermenu .signout{ padding:9px 14px } .seg button{ padding:9px 16px; font-size:13px } .statuschip{ padding:8px 12px }`. Keeps them within the wrapped actions row but finger-friendly. No JSX change.

[M] Layout.tsx @ styles.css .topnav a (lines 236-253) padding 8px 13px → ~33px tall; .topnav flex-wrap:wrap
    PROBLEM: When the topbar wraps below 1040px the nav becomes a full-width left-aligned wrapping row of pill links. For an officer there are up to 6 links (Home, Queue, Case Review, Proactive, Replay + numbers); at 320-375px these wrap to 2-3 rows of ~33px-tall pills, which is functional but the pills are slig
    FIX: Optional, in `@media (max-width:600px)`: `.topnav{ gap:6px } .topnav a{ padding:10px 14px }` to enlarge tap targets. The wrap already handles overflow, so this is a polish-only change; no markup change required.

[C] MyCase.tsx @ MyCase.tsx line 67-70: <div className="grid" style={{ gridTemplateColumns: "2fr 1fr", marg
    PROBLEM: This is an INLINE 2-column grid (ProfileCard 2fr + status card 1fr). The existing media query at styles.css:1131 only targets .grid-2/.grid-3/.grid-4 classes, so this inline grid never collapses. On a 320-375px phone the status card is squeezed to ~1/3 of ~300px (~95px), and the ProfileCard's inner 
    FIX: Because the columns are set inline they cannot be overridden by a class-based media query alone (inline styles win). Preferred: change the JSX to use a class instead of inline columns, e.g. className="grid grid-2" (keep style={{marginTop:16}}), so the existing @media (max-width:900px){.grid-2{grid-t

[M] MyCase.tsx @ MyCase.tsx line 52: <div className="grid" style={{ gridTemplateColumns: "1fr auto", alignI
    PROBLEM: The Case ID input and Load button sit in a 2-col '1fr auto' inline grid. The button (.btn padding 11px 20px) plus the input share one row; at 320px the input becomes narrow and on very small screens the button text + input can feel cramped. Minor at 320px but it never stacks.
    FIX: Acceptable in most cases since '1fr auto' keeps the button at content width. If you want it to stack, change JSX to a class and add @media (max-width:420px){ .stack-sm{ grid-template-columns:1fr; } .stack-sm .btn{ width:100%; } }. Optional/minor.

[M] MyCase.tsx @ styles.css:199-204 .brand .seal { height: 96px } (and .footer .f-brand .seal height:96px l
    PROBLEM: The brand seal is a fixed 96px tall image in the sticky topbar. On a 320-375px phone a 96px-tall logo consumes a large share of vertical space in the already-wrapping topbar (topbar wraps at 1040px per styles.css:1116) and pushes the sticky header very tall, eating viewport. The masthead Band on thi
    FIX: Add to a small-screen media query in styles.css: @media (max-width:600px){ .brand .seal{ height:56px; } .footer .f-brand .seal{ height:64px; } }. This is a global/shared chrome fix and benefits every page.

[M] MyCase.tsx @ components/ui.tsx DocumentPreview line 56-91 — <object ... height={height} default 360>, r
    PROBLEM: The embedded PDF <object> is width:100% (good) but a fixed 360px tall, and PDFs render at desktop page width inside, forcing the user to pinch/scroll a tiny document on a 320px phone. Mobile browsers also frequently fail to render <object>/<iframe> PDFs inline, leaving a 360px blank box. The 'open i
    FIX: This needs a markup/JSX change in ui.tsx: lower the default height on narrow screens (e.g. height={typeof window!=='undefined' && window.innerWidth<600 ? 220 : 360}) OR wrap the <object> so it is hidden under ~600px and only the 'open in new tab ↗' link shows. Cannot be fixed purely from styles.css 

[M] MyCase.tsx @ components/ui.tsx DecisionBadge line 234-237 + styles.css .determination .stamp-slot {posi
    PROBLEM: The decision stamp is absolutely positioned in the top-right corner of the determination panel; the .verdict heading (clamp 26-36px) sits in normal flow. On a 320-375px screen a long stamp label like 'Refer to Officer' (.stamp.lg, white-space:nowrap) overlaps or collides with the verdict text and th
    FIX: In styles.css add: @media (max-width:560px){ .determination .stamp-slot{ position:static; margin-bottom:8px; } [dir=rtl] .determination .stamp-slot{ left:auto; } .determination .verdict{ max-width:none; } }. This makes the stamp flow above the verdict instead of overlapping.

[M] MyCase.tsx @ styles.css:803-804 .determination { padding: var(--s8) } i.e. 32px
    PROBLEM: The determination panel uses a fixed 32px padding on all sides. On a 320px viewport (main padding already clamps to 20px each side → ~280px content) 32px*2=64px of inner padding leaves only ~216px for the verdict text, cramping the large clamp(26px) heading.
    FIX: Add to @media (max-width:600px){ .determination{ padding: var(--s6); } } (24px) in styles.css. Also consider the same for .card (padding var(--s6)=24px) → var(--s4) at <=480px if cards feel tight.

[M] MyCase.tsx @ components/ui.tsx ProfileCard line 181-202 .spread row with serif name + 'No hardship / Ha
    PROBLEM: .spread does NOT wrap (no flex-wrap, unlike .row). The left block holds a 22px serif name and the right holds a no-wrap hardship stamp. On a 320-375px phone the long full name and a 'Hardship · medical_hardship' nowrap stamp compete on one non-wrapping row, forcing overflow or severe squeezing of th
    FIX: In styles.css add: @media (max-width:560px){ .spread{ flex-wrap:wrap; } }. This affects all .spread rows (also used in ConfidenceBlock and PlanCards headers) and lets the stamp drop below the title. Low risk since gap is already set.

[M] MyCase.tsx @ components/ui.tsx ConfidenceBlock line 363 & 376: <div style={{ fontFamily: 'var(--mono)',
    PROBLEM: Two big 32px mono percentage numbers. After the grid-2 collapses to 1 col (<=900px) these are fine width-wise, but 32px mono on a 320px card is large; combined with the .card 24px padding it is acceptable but on the edge. Mostly OK — flagged as minor for consistency with the metric .v (also 32px).
    FIX: Optional: wrap these in a class (e.g. .bignum) and add @media (max-width:480px){ .bignum{ font-size:26px; } }. Or convert inline fontSize to a class. Low priority.

[M] MyCase.tsx @ styles.css:759-768 .metric .v { font-size: var(--fs-xl) } = 32px (rendered 3-up in Profile
    PROBLEM: Metric tile value is a fixed 32px. The three metrics (income/installment/arrears) show AED amounts like 'AED 12,500'. While grid-3 collapses to 1 column at <=900px (good), on a narrow phone a single-column metric tile with 32px bold tabular AED figures is large but fits one-per-row. At 320px a long 
    FIX: Optional: @media (max-width:480px){ .metric .v{ font-size: var(--fs-lg); } } (24px) in styles.css to give breathing room. Minor.

[M] MyCase.tsx @ components/ui.tsx PolicyTable line 271-299 / ConfidenceBlock — no overflow wrapper, BUT th
    PROBLEM: Note for accuracy: MyCase's 'validation' section uses PolicyTable which is a 22px+1fr grid that wraps text fine — it does NOT use table.tbl, so there is no horizontal-scrolling-table problem on THIS page. Likewise .bar-row (200px 1fr 36px) is not used here. No fix needed; documented to avoid a false
    FIX: No change required for MyCase. (If table.tbl were used elsewhere it would need a .table-wrap{overflow-x:auto} wrapper, but it is not on this page.)

[M] MyCase.tsx @ components/ui.tsx Band/masthead — pre.raw evidence block (MyCase.tsx:108) and raw_text
    PROBLEM: pre.raw correctly uses white-space:pre-wrap and overflow-x:auto, so long raw document text wraps and does not force page-wide horizontal scroll. The masthead .ref (styles.css:507-513) is white-space:nowrap but contains short 'FILE · القضية / MIZAN' text, not long enough to overflow 320px. These are 
    FIX: No change required; pre.raw and .ref are mobile-safe as written.

[M] Proactive.tsx @ .masthead .top (styles.css lines 494-499), rendered by Band at Proactive.tsx line 25
    PROBLEM: The masthead header row is `display:flex; justify-content:space-between` with NO `flex-wrap`. On the right sits `.ref` which is `font-family: mono` with `white-space: nowrap` (lines 507-513) printing 'FILE · القضية' / 'MIZAN / ميزان'. On a 320-375px viewport the h1 title 'Insight · early-warning wat
    FIX: In styles.css add to the `@media (max-width: 600px)` block: `.masthead .top { flex-direction: column; gap: var(--s2); } .masthead .ref { text-align: left; white-space: normal; }` ( and `[dir="rtl"] .masthead .ref { text-align: right; }` ). This stacks the title above the file ref and lets the mono r

[M] Proactive.tsx @ Proactive.tsx line 26: <p className="lead" style={{ fontSize: 20 }}>
    PROBLEM: The `.lead` class is intentionally fluid (`font-size: clamp(18px, 2.2vw, 22px)`, styles.css line 566), but this inline `fontSize: 20` hard-overrides the clamp with a fixed 20px. It is not catastrophic on phones (20px is readable), but it defeats the design system's responsive type scale and `max-wid
    FIX: Remove the inline `style={{ fontSize: 20 }}` from line 26 and let the `.lead` clamp govern, OR if a slightly larger lead is desired keep the class default. This is a per-file JSX change (delete the inline style attribute).

[M] Proactive.tsx @ Proactive.tsx lines 40-50: <div className="spread" style={{ alignItems: 'flex-start' }}> w
    PROBLEM: `.spread` is `display:flex; justify-content:space-between` with `gap: var(--s3)` and NO `flex-wrap` (styles.css lines 964-969). The left child is the beneficiary name in `font-family: var(--serif); fontSize: 20` (inline, line 42) which does not wrap nicely, and the right child is a `.stamp` pill wit
    FIX: Add a responsive rule in styles.css `@media (max-width: 480px)` (or 600px): `.card .spread { flex-wrap: wrap; }` so the stamp drops below the name on narrow screens. Globally safer: `@media (max-width: 480px) { .spread { flex-wrap: wrap; } }`. Optionally also reduce the inline 20px name to a clamp v

[M] Proactive.tsx @ Proactive.tsx line 45 (and line 52): <div className="caption mono">{a.case_id}</div> and '
    PROBLEM: The case id is rendered with `.mono` (monospace) and driver strings are joined and shown inline (line 52). Monospace identifiers and comma-joined driver lists are long unbroken-ish strings; if a case_id or driver token contains no spaces it can push the card wider than a 320px viewport since nothing
    FIX: In styles.css add (outside or inside a small-screen media query) `overflow-wrap: anywhere;` to `.card .mono, .muted, .kv` — e.g. `@media (max-width: 600px) { .card { overflow-wrap: anywhere; } }` so long mono ids / driver tokens break instead of overflowing the card.

[M] Proactive.tsx @ .btn (styles.css lines 629-646), used at Proactive.tsx line 57 <button className="btn ghos
    PROBLEM: Touch-target check: `.btn` has `padding: 11px 20px` and `font-size: 14px` with `line-height` inherited from body (1.6). Computed height is roughly 14*1.6 + 22 ≈ 44px, which meets the 40px minimum, so this is acceptable. Not a defect — noting it was checked. (No fix required.)
    FIX: No change needed; `.btn` already clears ~44px tall. If you want extra safety on touch devices, add `@media (max-width: 600px) { .btn { min-height: 44px; } }` in styles.css.

[M] ui.tsx @ styles.css .bar-row, lines 915-921 (grid-template-columns: 200px 1fr 36px), and the respon
    PROBLEM: The bar-row keeps a fixed first column (200px desktop, 130px below 900px) plus a 36/32px number column and two 16px gaps. At 320-375px that fixed label column plus gaps consumes ~190px+, leaving the .bar-track almost no room — bars are crushed to a sliver and long mono .lbl text truncates/overflows 
    FIX: Add a <=480px (or <=600px) rule in styles.css that collapses .bar-row to two rows: e.g. `@media (max-width:480px){ .bar-row{ grid-template-columns: 1fr auto; } .bar-row .lbl{ grid-column: 1 / -1; } }` so the label sits on its own line above a full-width track + number. No JSX change needed.

[M] ui.tsx @ ui.tsx Band/masthead: .top is flex space-between (styles.css lines 494-499) with .ref usin
    PROBLEM: The masthead .top row puts the serif <h1> (clamp up to 56px) and the right-aligned mono .ref ('FILE · القضية' / 'MIZAN / ميزان') in one space-between flex row that never stacks. On 320-375px the nowrap .ref competes with the h1 for width; the ref can push out and cause horizontal overflow, and the t
    FIX: Add to styles.css: `@media (max-width:600px){ .masthead .top{ flex-direction: column; gap: var(--s2); } .masthead .ref{ text-align: left; white-space: normal; } }` so the file-ref drops below the heading and wraps. No JSX change required.

[M] ui.tsx @ styles.css .brand .seal, lines 199-204 (height: 96px) and .footer .f-brand .seal line 406-
    PROBLEM: The brand seal image is a fixed 96px tall in the sticky topbar. On phones this oversized logo dominates the 56px-ish topbar height, forces the topbar very tall, and combined with brand .word can crowd the row before the nav wraps. The footer brand seal is also 96px. No responsive reduction exists.
    FIX: Add to styles.css: `@media (max-width:600px){ .brand .seal{ height: 48px; } .footer .f-brand .seal{ height: 64px; } }` (tune as desired). Global media query, no JSX change.

[M] ui.tsx @ styles.css .topbar-actions, lines 267-272 (display:flex; gap; flex:none) containing seg + 
    PROBLEM: At <1040px the topnav drops to its own full-width row, but .topbar-actions (language .seg + .statuschip + .usermenu with avatar+name+role+signout) stays on the brand row as a single non-wrapping flex group with flex:none. On 320-400px the brand + this actions cluster exceeds viewport width and cause
    FIX: Add to styles.css: `@media (max-width:600px){ .topbar{ row-gap: var(--s2); } .topbar-actions{ flex-wrap: wrap; width: 100%; justify-content: space-between; } .usermenu .who{ display:none; } }` (optionally hide the name/role text to save space). Global media query; no JSX change.

[M] ui.tsx @ styles.css .footer .f-logos, lines 461-474 (display:inline-flex; gap: var(--s8)=32px; img 
    PROBLEM: Two 84px-tall logo chips with 20px horizontal padding plus a 32px gap easily exceed 320-375px width. .f-logos is inline-flex with no wrap, so the MOEI/42 logos overflow horizontally on phones. The f-base row wraps (flex-wrap line 457) but f-logos itself does not.
    FIX: Add to styles.css: `@media (max-width:600px){ .footer .f-logos{ flex-wrap: wrap; gap: var(--s4); } .footer .f-logos img{ height: 56px; padding: 8px 14px; } }`. Global media query; no JSX change.

[M] ui.tsx @ ui.tsx DocumentPreview, lines 81-89: <object data type="application/pdf" width="100%" heig
    PROBLEM: Width is 100% (good) but the embedded PDF <object>/<iframe> has a fixed 360px height. On many mobile browsers inline PDF <object> rendering is unreliable/blank and 360px of dead space is wasteful on a small viewport; the 'open in new tab' link is the real escape hatch on phones. Not an overflow bug,
    FIX: Reduce height on small screens. Simplest global approach: give the object/iframe a class (JSX change) and add `@media (max-width:600px){ .pdf-preview{ height: 240px; } }`. Alternatively pass a smaller height prop from callers on mobile. Markup change required to add the hook class since the element 

[M] ui.tsx @ ui.tsx ProfileCard top row, lines 181-202: <div className="spread" style={{alignItems:'fle
    PROBLEM: The .spread flex row (styles.css 964-969) is space-between with only flex-wrap NOT set on .spread, and the long full name (22px serif) plus the nowrap hardship stamp can collide and push the stamp off-edge at 320px. .spread never wraps, unlike .row.
    FIX: Make .spread wrap on small screens globally: `@media (max-width:480px){ .spread{ flex-wrap: wrap; } }`. This also benefits PlanCards and the Confidence/Pipeline header spreads. No JSX change.

[M] ui.tsx @ ui.tsx ProfileCard financial metrics, lines 203-210: <div className="grid grid-3"> of <Met
    PROBLEM: grid-3 correctly collapses to 1 column at <=900px (styles.css 1131-1136), so layout is fine. But each Metric .v renders AED currency at 32px bold tabular (e.g. 'AED 1,250,000'). In the full-width single column on a 320px phone a large AED figure at 32px can still overflow the metric tile horizontall
    FIX: Shrink the metric value on phones: `@media (max-width:480px){ .metric .v{ font-size: var(--fs-lg); } }` (24px). Global media query; no JSX change.

[M] ui.tsx @ ui.tsx ConfidenceBlock, lines 355-381: outer <div className="grid grid-2"> and two inline 
    PROBLEM: grid-2 collapses to 1 col at <=900px (fine). The inline 32px mono percentage is small content so it's ok, but the risk card 'Drivers: ...' line (line 379) joins drivers with ', ' as a single mono-ish string that can be a long unbroken token causing minor overflow on 320px. Low impact.
    FIX: Ensure long driver/reason strings wrap by adding `overflow-wrap:anywhere` to .caption globally, e.g. add to .caption rule or `@media (max-width:480px){ .caption{ overflow-wrap:anywhere; } }`. No JSX change.

[M] ui.tsx @ styles.css .determination padding: var(--s8)=32px (lines 799-807); verdict clamp(26px,4vw,
    PROBLEM: On a 320px screen the determination card keeps 32px padding all around (content width ~256px), and the absolutely positioned decision stamp (top:22px right:22px) can overlap the eyebrow/verdict text since the verdict has no right padding reserved for it. The verdict min font 26px is acceptable but c
    FIX: Reduce padding and un-absolute the stamp on small screens: `@media (max-width:480px){ .determination{ padding: var(--s4); } .determination .stamp-slot{ position: static; margin-bottom: var(--s2); } }`. Global media query; no JSX change.

[M] ui.tsx @ styles.css table.tbl (lines 868-907) — shared table style used by this widget set (e.g. Re
    PROBLEM: table.tbl is width:100% with multiple mono columns and 13px/16px cell padding. With several columns of mono data it cannot shrink below its content width on a 320-375px phone, so it overflows horizontally and pushes the whole page wider (no overflow wrapper). There is no responsive handling for tabl
    FIX: Tables need a horizontal-scroll wrapper. Preferred markup change: wrap each <table className="tbl"> in <div className="tbl-wrap"> at the call sites, and add global CSS `.tbl-wrap{ overflow-x:auto; -webkit-overflow-scrolling:touch; }`. If markup changes are undesirable, a partial global fallback is `

[M] ui.tsx @ styles.css pre.raw (lines 1010-1021, white-space:pre-wrap; overflow-x:auto) — shared, used
    PROBLEM: pre.raw already uses pre-wrap + overflow-x:auto so it mostly behaves, but extracted OCR text can contain long unbroken tokens (URLs/IDs) that pre-wrap won't break, producing horizontal scroll inside the pre on phones. Minor since overflow-x:auto contains it to the box, but it can still nudge layout 
    FIX: Add `overflow-wrap:anywhere` (or `word-break:break-word`) to pre.raw so long tokens wrap on phones. Global single-line change to the pre.raw rule.

[M] ui.tsx @ Global: no <=480px tier exists. styles.css media queries stop at 1040 / 900 / 600 (lines 1
    PROBLEM: The smallest horizontal padding is clamp min 20px on .main and .topbar, i.e. 40px lost to gutters on a 320px screen (content ~280px). That min is borderline but acceptable; the bigger gap is that there is no dedicated phone breakpoint (<=480px) to tighten the items flagged above (bar-row, seal, metr
    FIX: Introduce a `@media (max-width:480px)` block grouping the phone-specific fixes (bar-row collapse, .metric .v size, .spread wrap, .determination padding/static stamp, .masthead .top stacking). Optionally lower the .main padding min to 16px: the clamp is already mobile-appropriate, so this is optional

[M] NewRequest.tsx @ NewRequest.tsx lines 248-262: section-title div with inline style {display:'flex', justify
    PROBLEM: The base .section-title (styles.css:536-553) is itself display:flex with a ::after flex:1 hairline. Here the JSX inline style overrides it to space-between with no flex-wrap, putting the section text + a flex:1 rule + a non-wrapping button in one row. On 320-375px the heading text ('2 · Documents') 
    FIX: Add a global rule in styles.css under a new @media (max-width: 600px) block: `.section-title{flex-wrap:wrap;}` so the button drops to its own line. Because the problematic flex/justify is an INLINE style on this element, a media query cannot override it — so a JSX change is required: remove the inli

[M] NewRequest.tsx @ styles.css:759-768 (.metric .v) applied to ProfileCard metrics (NewRequest.tsx:241-246 via
    PROBLEM: `.metric .v` is a fixed `font-size: var(--fs-xl)` = 32px with `letter-spacing:-0.02em` and tabular-nums. After grids collapse to 1 column at 900px the tile is full width, but long values like 'AED 12,500' or 'AED 1,250,000' at 32px can still crowd / overflow the tile padding on a 280px-wide content 
    FIX: In a styles.css `@media (max-width: 480px)` block add `.metric .v{font-size:26px;}` (or use `font-size:clamp(24px,7vw,32px)` on the base rule). No markup change needed.

[M] NewRequest.tsx @ NewRequest.tsx:407-415, the results block `<div className="grid grid-3">` containing a sin
    PROBLEM: Only one Metric ('Processing time') is rendered inside a grid-3. On desktop this leaves it stretched across 1/3 width, but on mobile it collapses fine via the 900px rule. This is not a breakage per se — flagging because grid-3 with one child is harmless on mobile. (No fix strictly required.)
    FIX: Optional: since there is only one Metric, drop the grid-3 wrapper or use grid-2 so the lone tile doesn't look oddly narrow on tablet/desktop. No mobile-specific fix needed; the existing @media(max-width:900px){.grid-3{grid-template-columns:1fr}} already makes it full width on phones.

[M] NewRequest.tsx @ ui.tsx:179-202 ProfileCard `.spread` row (rendered by NewRequest.tsx:241-246)
    PROBLEM: `.spread` (styles.css:964-969) is flex with justify-content:space-between and NO flex-wrap, holding the name/Arabic/ID block on the left and the 'Hardship · …' stamp on the right. The stamp has `white-space:nowrap` (styles.css:592). On 320-375px the long left text plus a non-wrapping hardship stamp 
    FIX: Add to styles.css `@media (max-width:600px){.spread{flex-wrap:wrap;}}` so the hardship stamp wraps under the identity block. This is a global, no-markup-change fix and also helps every other .spread usage (PipelineProgress header, PlanCards, ConfidenceBlock).

[M] NewRequest.tsx @ ui.tsx:111-116 Band 'ref' (`<div className="ref">`) rendered at top of NewRequest.tsx:233;
    PROBLEM: `.masthead .top` is a non-wrapping flex with the big clamp h1 (clamp(34px,5.5vw,56px)) on the left and the nowrap mono `.ref` ('FILE · القضية / MIZAN / ميزان') on the right. On 320-375px the h1 wraps to several lines but the nowrap ref is pinned right; the combination is cramped and the ref's fixed 
    FIX: Add to styles.css `@media (max-width:600px){.masthead .top{flex-direction:column;}.masthead .ref{text-align:left;white-space:normal;}}` ([dir=rtl] already flips text-align). Global, no markup change.

[M] NewRequest.tsx @ ui.tsx:233-250 DecisionBadge `.determination` with `.stamp-slot` (rendered NewRequest.tsx:
    PROBLEM: .determination has `padding: var(--s8)` (32px) on all sides (styles.css:799-807) and an absolutely-positioned `.stamp-slot` at top:22px right:22px (styles.css:823-827). On a 280px content width the 32px padding eats 64px, and the absolutely-positioned 'Refer to Officer'/'Rejected' stamp (lg, white-s
    FIX: Add to styles.css `@media (max-width:600px){.determination{padding:var(--s4);}.determination .stamp-slot{position:static;margin-bottom:8px;}.determination .verdict{max-width:none;}}` so the stamp flows above the verdict instead of overlapping, and padding shrinks. Global, no markup change.

[M] NewRequest.tsx @ NewRequest.tsx:325-341 uploaded-files `.card` rows and 273-278/328-331 `.kv` rows containi
    PROBLEM: Each uploaded-file row is a single inline `.kv` line: '📎 <b>Bank statement</b> · somelongfilename.pdf · <code>verified</code>'. Long file names and the mono `<code>` status are inline with no wrapping control; a long, unbroken file name (no spaces) will not wrap and will overflow the card horizontal
    FIX: Add to styles.css `.kv{overflow-wrap:anywhere;word-break:break-word;}` (and optionally `code{overflow-wrap:anywhere;}`). Global, no markup change. This protects every inline .kv data row on the page from long mono strings/file names.

[M] NewRequest.tsx @ ui.tsx:480-490 PipelineProgress step rows and 273-301 PolicyTable rows: inline `style={{di
    PROBLEM: These 22px + 1fr two-column grids are mobile-safe in column count, BUT the second (1fr) cell holds long labels (PolicyTable: '<b>{rule_id}</b> - {title}' with possibly long rule IDs/titles and a muted detail line). Without word-break, a long unbroken rule_id or detail string overflows the 1fr track 
    FIX: Covered by the global `.kv{overflow-wrap:anywhere}` fix above (PipelineProgress rows and PolicyTable rows both use className="kv"). No additional markup change; just ensure the .kv break rule is added to styles.css.

[M] NewRequest.tsx @ styles.css:380-386 `.main` padding `clamp(28px,5vw,56px) clamp(20px,5vw,64px) 96px` — page
    PROBLEM: Horizontal min padding is 20px each side, leaving 280px usable on a 320px phone. That is acceptable, but the 96px fixed bottom padding and 28px min top are larger than needed on small phones and combined with full-width cards make vertical rhythm loose. This is minor — the 20px side min is mobile-ap
    FIX: Optional polish: `@media (max-width:480px){.main{padding-left:16px;padding-right:16px;padding-bottom:56px;}}` to reclaim ~8px of width per side for the wide metric/file rows. Not required for correctness.

[M] NewRequest.tsx @ NewRequest.tsx:283-322 drag-and-drop upload `<label>` with inline `padding:'26px 18px'`
    PROBLEM: The dropzone uses fixed 26px/18px padding and centered text; this is fine and fluid (no fixed width). The inner glyph fontSize:26 and 'Drag & drop all your documents here' (fontWeight:600) wrap acceptably. No real breakage — noted only to confirm it is responsive.
    FIX: No change required. The dropzone is fluid width and wraps; touch target is large. If desired, reduce padding to '18px 14px' under @media(max-width:480px).

[M] NewRequest.tsx @ NewRequest.tsx:253-261 'Start new application' button uses inline `style={{padding:'6px 12
    PROBLEM: This button overrides the .btn default padding (11px 20px) down to 6px vertical → roughly ~28-30px tall, below the 40px recommended touch-target minimum on phones. The primary run button (line 367) and 'View full result' Link buttons use default .btn (~40px) and are fine.
    FIX: Since padding is an inline style, a media query can't easily override it. Either remove the inline `padding/fontSize` (let .btn defaults give a 40px target) — a JSX change — or add `@media(max-width:600px)` rule targeting the proposed `.section-title-actions .btn{min-height:40px;padding:9px 14px;}`.

